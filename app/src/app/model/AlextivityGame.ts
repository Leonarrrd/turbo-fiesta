import { NumberValueAccessor } from '@angular/forms';

export class AlextivityGame {
    teamRedPoints: number;
    teamBluePoints: number;
    wordPoolSize: number;
    remainingWordsThisRound: number;
    phase: string;
    activePlayer: string;

    constructor(){
    }
}